{
  "name": "magnitude",
  "version": "3.1.0",
  "private": true,
  "description": "MAGNITUDE (Slow Variables) — finds the order-of-magnitude gaps: value vs. capture, consensus vs. reality, price vs. truth. Structural constraints, tech-tree causality, high-agency bets, and market windows, with schema-enforced falsifiability.",
  "scripts": {
    "validate": "node js/validate.js",
    "serve": "npx serve -p 5173 --no-clipboard .",
    "og-image": "node scripts/generate-og.js",
    "patrol": "node scripts/patrol-report.js"
  },
  "devDependencies": {
    "canvas": "^3.2.3"
  }
}
